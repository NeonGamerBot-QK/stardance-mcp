{
  "name": "stardance-mcp",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun build ./index.ts --outdir dist --target node",
    "build:exe": "bun build ./index.ts --compile --outfile dist/stardance-mcp",
    "extract-routes": "bun run scripts/extract-routes.ts",
    "test": "bunx @modelcontextprotocol/inspector bun index.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "cheerio": "^1.2.0",
    "zod": "^4.4.3"
  }
}
